import 'package:flutter/material.dart';

class EquipoAgregar extends StatelessWidget {
  const EquipoAgregar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}